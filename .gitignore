node_modules/
.next/
/out/
/build/

# Segredos. .env.example é a única exceção versionada.
.env
.env.*
!.env.example

*.tsbuildinfo
next-env.d.ts
.vercel
.DS_Store

# Config local de agentes. O bloco anterior foi gravado em UTF-16 por
# engano, então o Git lia bytes nulos como lixo e nada aqui era ignorado —
# foi assim que .agents/ inteiro acabou versionado.
.agents/mcp_config.json

# Scratch e artefatos de depuração.
scratch/
err.html
*.log
